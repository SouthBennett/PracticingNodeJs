<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EJS Practice</title>
</head>
<body>
  <h1>EJS Practice</h1>
  <h2>Hello, <%= person.name %></h2>
  <h3>Occupation: <%= person.occupation %></h3>
  <h4>Favorite Color: <%= person.color %></h4>

  <% if (person.occupation === 'Plumber') { %>
    <p>You work with pipes!</p>
  <% } else { %>
    <p>That's and interesting job!</p>
  <% } %>

  <% if (person.color === 'red') { %>
    <p>Red is a good color</p>
  <% } else { %>
    <p>Thats a good color too</p>
  <% } %>



  <!-- Include partials -->
   <%- include ('partials/header') %>
   <main>
    <h2>Welcome!</h2>
   </main>
   <%- include ('partials/footer') %>

  
</body>
</html>